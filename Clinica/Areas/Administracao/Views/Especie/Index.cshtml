@model IEnumerable<Clinica.Models.Especie>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeEspecie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeEspecie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EspecieID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EspecieID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EspecieID })
        </td>
    </tr>
}

</table>
